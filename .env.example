# Optional local overrides. Copy to `.env` and load however you prefer
# (this project reads PORT and MOTOR_USE_SUDO from the process environment).
#
# PORT=3000
# MOTOR_USE_SUDO=1
#
# Verbose terminal logs for every GET /api/display/state (noisy if the display polls):
# STEPPERHOLO_DEBUG_STATE=1
